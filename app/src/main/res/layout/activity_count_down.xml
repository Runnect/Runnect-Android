<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".CountDownActivity"
        android:background="@drawable/starbackground">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/is_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/num3" />

        <androidx.appcompat.widget.AppCompatTextView
            android:textSize="18sp"
            android:textColor="@color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/is_count"
            app:layout_constraintStart_toStartOf="@id/is_count"
            app:layout_constraintEnd_toEndOf="@id/is_count"
            android:layout_marginTop="35dp"
            android:text="잠시 후 러닝을 시작합니다." />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>